const TaskName = ({ name }: { name: string }) => {
    return (
        <p className="
            w-[20vw]
            text-lg
            break-all
        " >{name}</p>
    );
};

export default TaskName
