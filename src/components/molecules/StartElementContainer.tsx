const StartElementContainer = ({children}: { children: React.ReactNode }) => {
    return (
        <div className="
        min-h-10
        mr-5
        ">{children}</div>
    );
};

export default StartElementContainer